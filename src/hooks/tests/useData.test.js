import { useData } from "../../hooks/useData";
import { renderHook, act } from "@testing-library/react";

test("should extract a standings table from fixtures array", () => {
  expect(true).toBe(true);
});
